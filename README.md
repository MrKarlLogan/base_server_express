Базовая реализация сервера на Node.js с фреймворком Express и TS с подключением к локальной MongoDB.

Запуск:
npm i
mongod --dbpath "C:/data/db" (Если не запущена и не выполнен connect в графической версии)
npm run dev (Старт сервера)
